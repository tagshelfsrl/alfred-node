export type CompanyInfo = {
  companyId: string;
  companyName: string;
};

export type Timestamps = {
  creationDate: Date;
  updateDate: Date;
};

export type EventProperties = {
  eventId: string;
  eventTime: number;
};
